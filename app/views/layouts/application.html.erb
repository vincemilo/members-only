<!DOCTYPE html>
<html>
  <head>
    <title>MembersOnly</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
      <% if user_signed_in? %>
            <%= link_to "New Post", new_post_path, class: "nav-button inverted" %>
            <%= link_to "Log Out", destroy_user_session_path, data: { turbo_method: :delete }, class: "nav-button" %>
          <% else %>
            <%= link_to "Sign In", new_user_session_path, class: "nav-button" %>
            <%= link_to "Sign Up", new_user_registration_path, class: "nav-button" %>
      <% end %>

    <%= yield %>
  </body>
</html>
